# Legacy files

This directory contains the pre-Vite static site files retained for reference:

- assets/ — the former standalone CSS, JavaScript, and image files
- scripts/ — the former deployment/search-engine helper scripts

The active application uses src/ and public/assets/.
